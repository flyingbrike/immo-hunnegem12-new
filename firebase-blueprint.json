{
  "entities": {
    "GalleryImage": {
      "title": "Gallery Image",
      "description": "An image in the property gallery",
      "type": "object",
      "properties": {
        "url": { "type": "string", "description": "The URL of the image" },
        "title": { "type": "string", "description": "The title or caption of the image" },
        "createdAt": { "type": "string", "format": "date-time", "description": "When the image was added" },
        "ownerId": { "type": "string", "description": "The ID of the user who added the image" }
      },
      "required": ["url", "title", "createdAt", "ownerId"]
    }
  },
  "firestore": {
    "gallery": {
      "schema": "GalleryImage",
      "description": "Collection of gallery images"
    },
    "surroundings_gallery": {
      "schema": "GalleryImage",
      "description": "Collection of images showing the surroundings/neighborhood"
    }
  }
}
